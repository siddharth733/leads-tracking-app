version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DATABASE_URL=file:./prisma/dev.db
    volumes:
      - sqlite_data:/app/server/prisma

volumes:
  sqlite_data:
